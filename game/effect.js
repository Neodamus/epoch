function EFFECT(type, target, value) {
	
	// setup variables
	this.type = type;			// attack, damage over time, move
	this.targets = targets;		// targets affected
	this.values = values;		// effect value	
}